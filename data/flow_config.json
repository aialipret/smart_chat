{
  "flow_config": {
    "created_at": "2024-10-27T12:00:00Z",
    "description": "Workflow for creating new bank accounts.",
    "expected_outputs": [
      "New bank account created",
      "Account confirmation email sent"
    ],
    "flow_logic": "Sequential flow from step1 to step4.  If data validation fails in step2, the workflow should terminate.",
    "steps": [
      {
        "description": "Gather necessary information from the customer.",
        "name": "Collect Customer Data",
        "next_step": "step2",
        "parameters": {
          "fields": [
            "name",
            "second_name",
            "age",
            "id_number"
          ]
        },
        "step_id": "step1",
        "type": "input"
      },
      {
        "description": "Validate the collected customer data.",
        "name": "Data Validation",
        "next_step": "step3",
        "parameters": {
          "validation_rules": {
            "age": "must be above 18",
            "id_number": "must be a valid ID format"
          }
        },
        "step_id": "step2",
        "type": "processing"
      },
      {
        "description": "Create a new bank account.",
        "name": "Account Creation",
        "next_step": "step4",
        "parameters": {
          "database_connection": "bank_db"
        },
        "step_id": "step3",
        "type": "processing"
      },
      {
        "description": "Send account confirmation to the customer.",
        "name": "Account Confirmation",
        "next_step": null,
        "parameters": {
          "confirmation_method": "email"
        },
        "step_id": "step4",
        "type": "output"
      }
    ],
    "system_instructions": "Follow each step sequentially.  For step 2, implement robust validation checks based on the parameters.  If validation fails, log the error and terminate the workflow.  Step 3 requires a secure database connection to create the account. Step 4 should send a confirmation email including account details. Ensure all data handling adheres to privacy regulations.",
    "triggers": [
      "User initiates account creation"
    ],
    "version": "1.0",
    "workflow_name": "Bank Account Creation"
  },
  "created_at": "2025-09-19T11:03:25.965513",
  "version": "1.0"
}